'use client';

import React from 'react';
import { StudentWorkspace } from '@/components/roles/StudentWorkspace';

export default function StudentSubmitPage() {
  return <StudentWorkspace view="submit" />;
}
